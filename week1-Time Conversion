class Result {
   public static String timeConversion(String s) {
       String a=s.substring(0,2);
       int i=Integer.parseInt(a)%12;
       if(s.endsWith("PM"))
       i+=12;
        return String.format("%02d",i)+s.substring(2,8);

    }

}
